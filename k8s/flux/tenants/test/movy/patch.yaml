apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: movy
  namespace: movy-test
spec:
  # needed for Flux to be able to "patch" immutable resources such as StatefulSets
  targetNamespace: movy-test
  path: ./k8s/plain
